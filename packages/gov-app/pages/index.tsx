import type { NextPage } from "next";
import { Header, Layout } from "@gov-app/libs/components";

const Home: NextPage = () => {
	return (
		<Layout>
			<Header />
		</Layout>
	);
};

export default Home;
